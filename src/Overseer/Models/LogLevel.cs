using System;
using System.Collections.Generic;
using System.Text;

namespace Overseer.Models
{
    public enum LogLevel
    {
        TRACE = 0,
        DEBUG,
        INFO,
        LOG,
        WARN,
        ERROR,
        FATAL,
        OFF
    }
}
